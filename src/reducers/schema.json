{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicsNgxDemoReducer",
  "title": "NgxDemo Reducer Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the reducer.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Name of the reducer please ?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the reducer.",
      "visible": false
    }
  },
  "required": [
    "name"
  ]
}